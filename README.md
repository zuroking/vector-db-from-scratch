# vector-db-from-scratch
Self-contained vector database built from scratch on pure numpy — no FAISS, hnswlib, or ANN libraries. Custom HNSW index, binary persistence format, CLI, and benchmarks. Built under a strict decision-locking process with mutation-tested core logic and 100% test coverage across 144 tests. Portfolio project prioritizing algorithmic depth.
